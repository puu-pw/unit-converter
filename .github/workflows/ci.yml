name: Lint Code with Pylint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: windows-latest

    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # # Install dependencies
      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install pylint
      #     # Optionally install other dependencies if needed, like from requirements.txt
      #     # pip install -r requirements.txt

      # Run Pylint
      - name: Run Pylint
        run: |
          pylint unit-converter.py
